import forEach from './forEach'
import {
  singleArgumentPipe,
  multiArgumentPipe
} from './pipe'
import compose from './compose'
import asyncMap from './map'

export {
  forEach,
  asyncMap,
  singleArgumentPipe,
  multiArgumentPipe,
  compose
}
